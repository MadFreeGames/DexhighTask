using System.Collections;
using UnityEngine;
using UnityEngine.UI;

// Status controller script that handles the character's status
public class StatusController : MonoBehaviour
{
    // Reference to the health bar component
    public HealthBar healthBar;

    // Reference to the mana bar component
    public ManaBar manaBar;

    // Reference to the slot controller component
    public SlotController slotController;

    // Note: This script seems to be a container for other status-related components.
    // It does not have any methods or logic, it simply holds references to other components.
}
